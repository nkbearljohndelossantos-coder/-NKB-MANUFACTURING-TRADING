{
  "name": "client-order-delivery-variance-billing-system",
  "version": "1.0.0",
  "description": "Production-ready B2B Client Order, Delivery, Variance, Billing and Payment Management Web Application",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm run start --prefix backend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && node -e \"const fs=require('fs');fs.cpSync('frontend/dist','.',{recursive:true});\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "test": "npm run test --prefix backend"
  },
  "keywords": [
    "b2b",
    "order-management",
    "delivery",
    "variance",
    "billing",
    "erp"
  ],
  "author": "NKB Enterprise IT Engineering",
  "license": "ISC"
}
